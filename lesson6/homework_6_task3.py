some_text = "Пал, а норов худ и дух ворона лап."


def pal_check(str)
    new_str = str.replace(' ', '')
    new_str = new_str.replace(',', '')
    new_str = new_str.replace('.', '')
    l = len(str)
    l = l / 2
    for i in new_str:
        if
